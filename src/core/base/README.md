# core/base

Kelas asas, error types, dan helper HTTP yang dikongsi antara features.

Contoh simpanan kat sini:
- Custom error classes (e.g. `AppError`)
- Helper untuk bungkus `hono/http-exception` (HTTPException)
- Type helper yang reusable